import {redirect} from "next/navigation";import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {createContract,renewContract,transitionContract} from "./actions";
export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const [{data:rows},{data:props},{data:orgs}]=await Promise.all([s.from("contracts").select("id,contract_number,name,status,start_date,end_date,contract_value,property_id").order("end_date",{ascending:true,nullsFirst:false}).limit(200),s.from("properties").select("id,name").order("name"),s.from("organizations").select("id,name").order("name").limit(200)]);return <main className="list-shell"><header className="list-header"><Link className="back" href="/dashboard">← Command</Link><span className="eyebrow">REVENUE RETENTION</span><h1>Contracts</h1></header><section className="table-panel"><h2>Create contract</h2><form action={createContract} className="form-grid"><input name="name" required placeholder="Contract name"/><select name="property_id" required><option value="">Property</option>{props?.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><select name="organization_id" required><option value="">Customer</option>{orgs?.map(o=><option key={o.id} value={o.id}>{o.name}</option>)}</select><input name="start_date" type="date" required/><input name="end_date" type="date"/><input name="contract_value" type="number" step="0.01" required placeholder="Value"/><input name="billing_frequency" placeholder="Billing frequency"/><input name="renewal_type" placeholder="Renewal type"/><button>Create draft</button></form></section><section className="table-panel"><div className="table-wrap"><table><thead><tr><th>Contract</th><th>Status</th><th>Property</th><th>End</th><th>Actions</th></tr></thead><tbody>{rows?.map(r=><tr key={r.id}><td>{r.contract_number}<br/>{r.name}</td><td>{r.status}</td><td>{props?.find(p=>p.id===r.property_id)?.name??"—"}</td><td>{r.end_date??"—"}</td><td><form action={transitionContract} className="actions"><input type="hidden" name="id" value={r.id}/><select name="status">{["pending_signature","active","suspended","expired","renewal_pending","terminated","draft"].map(x=><option key={x}>{x}</option>)}</select><button>Transition</button></form><form action={renewContract} className="actions"><input type="hidden" name="id" value={r.id}/><input name="start_date" type="date" required/><input name="end_date" type="date"/><input name="contract_value" type="number" step="0.01" placeholder="Value"/><button>Renew</button></form></td></tr>)}</tbody></table></div></section></main>}